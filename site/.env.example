# Copy to `.env` for local Astro builds. For GitHub Actions, set repository Variables / Secrets.

# Required to pull recordings from Sanity at build time (omit to use bundled demo tracks).
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Optional: only if your dataset requires authenticated reads.
SANITY_READ_TOKEN=
